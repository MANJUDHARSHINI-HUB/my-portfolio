Drop your project screenshots into this folder (e.g. .png, .jpg).

Then reference them in src/data.js, inside the `projects` array, e.g.:

  image: "/projects/blood-pressure.png",

Until an image is added, a clean "Screenshot coming soon" placeholder is
shown automatically instead of a broken image icon.
